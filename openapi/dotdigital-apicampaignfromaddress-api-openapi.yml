openapi: 3.2.0
info:
  title: apiconnector.com API Campaign From Address API
  version: v2
servers:
- url: https://r1-api.dotdigital.com
tags:
- name: ApiCampaignFromAddress
paths:
  /v2/custom-from-addresses:
    get:
      tags:
      - ApiCampaignFromAddress
      summary: Gets all custom from addresses which can be used in a campaign.
      operationId: ApiCampaignFromAddress_GetCustomFromAddresses
      parameters:
      - name: select
        in: query
        schema:
          type: integer
          format: int32
          default: 1000
        x-position: 1
      - name: skip
        in: query
        schema:
          type: integer
          format: int32
          default: 0
        x-position: 2
      responses:
        '200':
          description: ''
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ApiCampaignFromAddress'
components:
  schemas:
    ApiCampaignFromAddress:
      type: object
      additionalProperties: false
      properties:
        id:
          type: integer
          format: int32
        email:
          type:
          - string
          - 'null'
x-generator: NSwag v14.4.0.0 (NJsonSchema v11.3.2.0 (Newtonsoft.Json v13.0.0.0))
